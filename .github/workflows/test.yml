name: "build-test"
on: # rebuild any PRs and main branch changes
  pull_request:
  push:
    branches:
      - master
      - test-ts

jobs:
  build:
    runs-on: ubuntu-latest
    outputs:
      output1: ${{ steps.step1.outputs.jekyllSrc }}
      output2: ${{ steps.step1.outputs.jekyllHash }}
    steps:
    - uses: actions/checkout@v2
    - id: step1
      name: Find jekyll directory
      run: |
        JEKYLL_SRC=$(find . -path ./vendor/bundle -prune -o -name _config.yml -exec dirname {} \; | tr -d '\n')
        echo "::set-output name=jekyllSrc::${JEKYLL_SRC}"
        echo "::set-output name=jekyllHash::ls -alR --full-time ${JEKYLL_SRC} | sha1sum"
    - name: Cache bundle files
      uses: actions/cache@v1
      with:
        path: vendor/bundle
        key: tstest-${{ runner.os }}-gems-${{ hashFiles('**/Gemfile.lock') }}
        restore-keys: |
          tstest-${{ runner.os }}-gems-
    - name: Cache jekyll files
      uses: actions/cache@v1
      with:
        path: ${{needs.build.outputs.output1}}/.jekyll-cache
        key: tstest-${{ runner.os }}-jekyll-${{needs.build.outputs.output2}}
        restore-keys: |
          tstest-${{ runner.os }}-jekyll-
    - name: Set up Ruby 2.6
      uses: ruby/setup-ruby@v1
      with:
        ruby-version: 2.6
    - uses: ./
    - name: Deploy
      # publish docs only when PR is merged on master
      if: github.event_name == 'push'
      uses: peaceiris/actions-gh-pages@v3
      with:
        github_token: ${{ secrets.GITHUB_TOKEN }}
        publish_dir: ./_site